const db_config = {
    protocol: "mongodb",
    host: "localhost",
    port: 4000,
    database: "trip_rating",
    username: "root",
    password: "root"
};

module.exports = {
    db_config: db_config
};